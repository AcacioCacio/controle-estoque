import { createTheme } from "@mui/material";

export default createTheme({
  palette: {
    primary: {
      main: "#e31e4d",
    },
    secondary: {
      main: "#0a504a",
    },
    background: {
      default: "#fff4f4",
      paper: "#fff",
    },
  },
});
