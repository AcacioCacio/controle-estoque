import { createTheme } from "@mui/material";

export default createTheme({
  palette: {
    primary: {
      main: "#1876D0",
    },
    background: {
      default: "#F5F5F5",
      paper: "#FFF",
    },
    error: {
      main: "#D01818",
    },
  },
});
